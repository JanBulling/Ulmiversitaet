---
import QuickSelectCard from "@/components/QuickSelectCard.astro";
import Layout from "@/layouts/Layout.astro";
import { quickSelect } from "@/content/quick-select";
import { cards } from "@/content/cards";
import LinkCard from "@/components/cards/LinkCard.astro";
import { announcements } from "@/content/announcements";
---

<Layout>
  <div class='py-8 px-2'>
    <h1 class='text-2xl md:text-5xl font-semibold text-primary-foreground'>
      <span class='text-primary font-bold'>Ulm</span>iversität
    </h1>
    <h2>Das inoffizielle Uni-Ulm Link-Portal</h2>

    <section
      class='grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 mt-8 gap-2 md:gap-4'
    >
      {quickSelect.map((quick) => <QuickSelectCard quickSelect={quick} />)}
    </section>

    <section class='bg-primary px-8 py-4 mt-16 rounded-md'>
      <h2
        class='py-1 px-2 bg-background w-max text-lg md:text-2xl font-semibold'
      >
        Ankündigungen
      </h2>
      <div class='space-y-2 mt-4'>
        {
          announcements.map((ann) => (
            <div class='bg-background px-2 py-1'>
              <h3 class='font-semibold'>{ann.title}</h3>
              {ann.description}
            </div>
          ))
        }
      </div>
    </section>

    <section class='mt-16'>
      <h2 class='text-lg md:text-2xl font-semibold text-primary-foreground'>
        Weitere nützliche Links
      </h2>
      <div
        class='grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-2 md:gap-4 mt-4'
      >
        {cards.map((card) => <LinkCard content={card} />)}
      </div>
    </section>
  </div>
</Layout>
